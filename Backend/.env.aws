# AWS RDS Configuration
# Copia este archivo a .env.aws y completa con tus credenciales

# Nombre de la base de datos en AWS RDS
AWS_DB_NAME=capstone_prod

# Usuario de PostgreSQL (generalmente 'postgres' o el que creaste)
AWS_DB_USER=postgres

# Contraseña del usuario (la que configuraste al crear la instancia RDS)
AWS_DB_PASSWORD=CapsFBT2025$

# Endpoint de tu instancia RDS (encuéntralo en AWS Console > RDS > Databases)
# Ejemplo: myinstance.c9akciq32.us-east-1.rds.amazonaws.com
AWS_DB_HOST=capstone-database.cm9q20moyrf7.us-east-1.rds.amazonaws.com

# Puerto de PostgreSQL (por defecto 5432)
AWS_DB_PORT=5432

# ================== CHATBOT / BACKEND ==================
# URL pública del backend (por ejemplo, el dominio de Elastic Beanstalk o Load Balancer)
# BACKEND_URL=https://tu-backend.amazonaws.com

# Clave API para que las acciones del chatbot consuman los endpoints /chatbot/* del backend
# CHATBOT_API_KEY=pon-una-clave-segura-aqui

# ============================================
# INSTRUCCIONES:
# ============================================
# 1. Ve a AWS Console > RDS > Databases
# 2. Selecciona tu instancia
# 3. En "Connectivity & security" encuentra el "Endpoint"
# 4. Copia el endpoint completo (sin el puerto) en AWS_DB_HOST
# 5. Usa la contraseña que configuraste al crear la instancia
# 6. Verifica que el Security Group permita tu IP en el puerto 5432
